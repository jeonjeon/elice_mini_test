name: elice_mini_test
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  device_preview: ^0.7.1
  sizer: ^1.1.8
  flutter_bloc: ^7.0.0
  google_fonts: ^2.0.0
  cupertino_icons: ^1.0.2
  http: ^0.13.1
  qr_code_scanner: ^0.4.0
  equatable: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/2.0x/
    - assets/images/3.0x/
